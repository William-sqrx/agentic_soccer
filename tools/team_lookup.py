from model.team import Team


def team_lookup(team_name: str) -> Team:
    # TODO: use the `./data/processed/team_stats.csv` to find the corresponding team's row and stats. Than create a Team instance and return
    pass
